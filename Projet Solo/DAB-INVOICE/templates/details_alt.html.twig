{% extends 'base_alt.html.twig' %}

{% block title %}Détails Clients{% endblock %}

{% block body %}
    <h1>{{ client.name }}</h1>
    <p><strong>Adresse:</strong> {{ client.adress }}</p>
    <p><strong>Email:</strong> {{ client.email }}</p>
    <p><strong>Created at:</strong> {{ client.created_at|date('Y-m-d H:i') }}</p>
    <p><strong>Archived at:</strong> {{ client.archived_at ? client.archived_at|date('Y-m-d H:i') : 'Not Archived' }}</p>

    <h2>Contacts</h2>
    <ul>
        {%for contact in client.contacts %}
           <li>{{ contact.firstname }} {{contact.lastname}} ({{ contact.role }}) - {{ contact.email }}</li>
        {% endfor %}
    </ul>
{% endblock %}
