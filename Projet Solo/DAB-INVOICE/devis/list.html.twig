{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <h1>{{ title }}</h1>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Nom du document</th>
                <th>Date de création</th>
                <th>Statut</th>
            </tr>
        </thead>
        <tbody>
            {% for devis in devis %}
                <tr>
                    <td>{{ devis.id }}</td>
                    <td>{{ devis.documentName }}</td>
                    <td>{{ devis.createdAt|date('d/m/Y') }}</td>
                    <td>{{ devis.status }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}
