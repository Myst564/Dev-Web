<aside class="scrollbar sidenav bg-white navbar navbar-vertical navbar-expand-xs border-0 border-radius-xl my-3 fixed-start ms-4 " id="sidenav-main">
    <div class="sidenav-header">
        <i class="fas fa-times p-3 cursor-pointer text-secondary opacity-5 position-absolute end-0 top-0 d-xl-none" aria-hidden="true" id="iconSidenav"></i>
        <a class="navbar-brand m-0" href="{{ path('home') }}">
            <img alt="Logo BASEO" src="{{ asset('build/img/logo.svg') }}">
        </a>
    </div>
    <div class="collapse navbar-collapse w-auto h-auto h-100" id="sidenav-collapse-main">
        <ul class="navbar-nav">
            <!-- Menu existant -->
            {{ knp_menu_render('menu_aside', {
                currentClass: 'active',
                'allow_safe_labels': true
            }) }}

            <!-- Ajout des sections Facturation et Devis -->
            <li class="nav-item">
                <a class="nav-link {% if app.request.attributes.get('_route') starts with 'facturation' %} active {% endif %}" href="{{ path('facturation_index') }}">
                    <i class="fa fa-file-invoice"></i>
                    <span class="nav-link-text">Facturation</span>
                </a>
            </li>

            <li class="nav-item">
                <a class="nav-link {% if app.request.attributes.get('_route') starts with 'devis' %} active {% endif %}" href="{{ path('devis_index') }}">
                    <i class="fa fa-file-contract"></i>
                    <span class="nav-link-text">Devis</span>
                </a>
            </li>
        </ul>
    </div>
</aside>
